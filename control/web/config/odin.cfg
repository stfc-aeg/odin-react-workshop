[server]
debug_mode = 1
http_port  = 8888
http_addr  = 127.0.0.1
static_path = web/static
adapters   = workshop
enable_cors = true

[tornado]
logging = debug

[adapter.workshop]
module = workshop.adapter.WorkshopAdapter